import authReducer, { loginUserThunk, logoutUserThunk, setRole, clearError } from '../auth/store/authSlice';
export default authReducer;
export { loginUserThunk, logoutUserThunk, setRole, clearError };
