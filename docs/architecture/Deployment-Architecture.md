# Deployment Architecture

## Build Pipeline
- Built using `vite build` targeting ESNext production output.
- Bundled into optimized static assets in `/dist`.
- Deployed to static hosting/CDN or containerized NGINX web server environments.
